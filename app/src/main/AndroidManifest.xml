<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.merlinsbeard.flashcardspro">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="false"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".activities.LaunchActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".activities.LoginActivity"
            android:label="Login"
            android:screenOrientation="portrait" />
        <activity
            android:name=".activities.CreateAccountActivity"
            android:label="Create Account"
            android:screenOrientation="portrait" />
        <activity
            android:name=".activities.SetViewActivity"
            android:label="Sets"
            android:screenOrientation="portrait"
            android:launchMode="singleTop"/>
        <activity
            android:name=".activities.AccountViewActivity"
            android:label="Account"
            android:screenOrientation="portrait" />
        <activity
            android:name=".activities.FlashCardView"
            android:label="Flashcards"
            android:screenOrientation="portrait"
            android:parentActivityName=".activities.SetViewActivity"
            android:launchMode="singleTop">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".activities.SetViewActivity"/>
        </activity>
        <activity
            android:name=".activities.ScrollView"
            android:label="Flashcards"
            android:screenOrientation="portrait"
            android:parentActivityName=".activities.FlashCardView">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".activities.FlashCardView"/>
        </activity>

    </application>

</manifest>